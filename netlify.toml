# Netlify configuration
# Static site — no build step.
# base = the subfolder Netlify works from; publish "." = that same folder
# (publish is resolved relative to base, so this avoids a doubled path).
[build]
  base = "landing-page-project"
  publish = "."
  command = ""
